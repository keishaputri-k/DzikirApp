<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kei.dzikirapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DzikirApp">
        <activity android:name=".activity.DetailArtikelActivity"></activity>
        <activity android:name=".activity.PetangActivity" />
        <activity android:name=".activity.PagiActivity" />
        <activity android:name=".activity.DzikirSetiapSaatActivity" />
        <activity android:name=".activity.DzikirPagiPetangActivity" />
        <activity android:name=".activity.DzikirHarianActivity" />
        <activity android:name=".activity.QauliyahActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>